# Proposed Changes

## Core

### Metadata
- [ ] Add "context_window" to the metadata so we know what context window size was used

### Summarize
- [ ] Allow the user to pass in additional context/instruction to the prompt
  - [ ] Store this in the db?
- [ ] Allow custom summaries (user-defined, specific prompts, quesetions, etc.)

## Viewer (GUI)

- [ ] Pop-up or widget supporting additional context/instruction upon re-summarize
